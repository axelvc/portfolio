---
import { Image } from 'astro:assets'
import Layout from '../layouts/Layout.astro'
import { marked } from 'marked'
import client from '../services/notion.ts'
const data = await client.getHomeData()
---

<Layout>
  <main class="mt-20">
    <div class="flex gap-6 sm:gap-10">
      <Image
        src="/pug.svg"
        alt=""
        aria-hidden="true"
        width="70"
        height="70"
        class="select-none pointer-events-none w-18 sm:w-[88px] h-auto"
      />
      <h1 class="text-3xl sm:text-5xl font-bold tracking-wider">
        I'm Axel
        <span class="block text-2xl sm:text-4xl text-base-400">Web Developer</span>
      </h1>
    </div>

    {data.map(block => <p class="paragraph" set:html={marked.parseInline(block)} />)}

    <div class="inline-flex items-center gap-8">
      <a
        href="https://cal.com/axel-vasquez-ddxxmd/15min"
        target="_blank"
        class="no-underline rounded px-3 py-2 tracking-wide transition-colors bg-base-800 border border-base-700 hover:bg-base-700"
      >
        Schedule a 15 min call
      </a>

      <a
        href="mailto:a.axelvc@gmail.com"
        rel="noopener noreferrer"
        target="_blank"
        class="no-underline rounded px-3 py-2 tracking-wide transition-colors bg-base-800 border border-base-700 hover:bg-base-700"
      >
        Contact me
      </a>
    </div>
  </main>
</Layout>
